//acciones necesarias cuando se cargue el componente
function detalle(id) {
    //window.alert(`Se ha cargado el componente de detalle con id: ${id}`)

    //consultar los datos de la película cuyo id llega en el parámetro de la función
    consultaDetalle(id)
}